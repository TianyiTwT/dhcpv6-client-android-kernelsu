# 变更记录

## v0.1.1

- 修复：看门狗不再把自己关掉。根因是它在接口尚未出现（开机时 Wi-Fi 未就绪）或
  模块被停用时调用了 `stop`，而 `stop` 会写入「用户暂停」标志并结束看门狗自身，
  于是后续每次开机都静默停在待命状态，表现为「重启后客户端不自启，手动点一次
  『重启客户端』才恢复」。新增 `hold` 子命令（只停止客户端，不置暂停标志、不结束
  看门狗），看门狗的两个分支改用它。
- 修复：`service.sh` 在开机时清除上一次会话残留的暂停标志。暂停属于运行期状态，
  不跨重启。
- 新增：看门狗进入待命、恢复巡检时写入日志。
- 文档：补充 `dhcp6c-ctl.sh` 的 `hold` 子命令；`build.sh` 支持 `--api`，
  与 CI 手动触发时的参数保持一致。

## v0.1.0

首个版本。AF_PACKET 二层收发、标准四步握手、DUID + 固定 IAID 的地址稳定性、
看门狗自动收敛、WebUI、模块卡片状态、开机自启。
